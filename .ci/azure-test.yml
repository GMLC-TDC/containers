steps:
  - script: ctest --output-on-failure -C $(containers.build_type) -T test
    displayName: "Test"
    workingDirectory: build

  - task: PublishTestResults@2
    inputs:
      testResultsFormat: "CTest"
      testResultsFiles: "**/Test.xml"
